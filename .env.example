# Vimeo API access token with the "private" scope, used to fetch official captions
# when a video already has them. Create one at https://developer.vimeo.com/apps.
VIMEO_ACCESS_TOKEN=

# OpenAI API key, used as a fallback to transcribe audio with Whisper when a
# video has no captions.
OPENAI_API_KEY=
